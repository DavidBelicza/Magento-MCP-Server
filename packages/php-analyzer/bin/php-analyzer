#!/usr/bin/env php
<?php

declare(strict_types=1);

use Magentic\PhpAnalyzer\Command\Parse;
use Symfony\Component\Console\Application;

require dirname(__DIR__) . '/vendor/autoload.php';

$application = new Application('Magentic PHP Analyzer');
$application->addCommand(new Parse());
$application->run();
