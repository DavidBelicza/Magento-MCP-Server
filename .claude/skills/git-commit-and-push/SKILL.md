---
name: git-commit-and-push
description: Use when the user asks to commit changes, prepare a git commit message, stage files, commit, or push changes while checking for generated, local-only, or sensitive files first.
---

Read `.agents/skills/git-commit-and-push/SKILL.md` and follow its steps exactly.

That file is the single source of truth for this project's commit and push
workflow, shared with other agent tools. Do not paraphrase or reimplement it here.
